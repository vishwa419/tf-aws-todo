apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend.fullname" . }}
spec:
  type: LoadBalancer
  selector:
    app: {{ include "frontend.name" . }}
  ports:
  - port: 80
    targetPort: 80

